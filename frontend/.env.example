# Frontend Configuration

This frontend is configured to communicate with the Flask backend at `http://localhost:5000`.

## Environment Variables

Create a `.env` file in the frontend root (optional):

```
VITE_API_BASE_URL=http://localhost:5000
```

By default, the API client uses `http://localhost:5000`.
